// verification-code-controller.js
const verificationCodeController = {
    sendVerificationCode: (req, res) => {
        console.log("sendVerificationCode was hit");
    }
}

module.exports = {
    verificationCodeController
}